<%= form_for(@setor) do |f| %>
  <% if @setor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setor.errors.count, "error") %> prohibited this setor from being saved:</h2>

      <ul>
      <% @setor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codsetor %><br>
    <%= f.number_field :codsetor %>
  </div>
  <div class="field">
    <%= f.label :nomesetor %><br>
    <%= f.text_field :nomesetor %>
  </div>
  <div class="field">
    <%= f.label :filial_id %><br>
    <%= f.select :filial_id, options_for_select(Filial.all.map{|m|[m.nomefilial, m.id]},f.object.filial_id) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
